read me the cooked

#git set up 
'''
git init

git remote add origin -link-

'''

##Git Notes
'''

'''
git branch
git add -A
git commit -m 'message'
git push origin master
tools 
command line
power shell

git checkout -b  dev 
new branch